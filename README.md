# 🐱 Focus Cat — Firefox WebExtension (Manifest V3)

> Минималистичное расширение для Firefox, которое помогает держать фокус: следит за активной вкладкой, считает время на «отвлекающих» сайтах и блокирует их при превышении лимита.

---

## ✨ Что умеет

- ✅ **Белый список доменов** (разрешённые сайты).
- ⏱️ **Таймер отвлечения** вне белого списка.
- 🚫 **Автоблокировка** после настраиваемого лимита времени вне фокуса.
- 🐾 **Сигнал кота** перед блокировкой (через content script).
- 🔌 **Глобальный переключатель ON/OFF**:
  - `ON` — логика расширения активна;
  - `OFF` — расширение полностью «на паузе».
- 💾 Состояние хранится в `browser.storage.local`:
  - `domains`
  - `enabled`
  - `lastAllowedTime`
  - `distractedTime`

---

## 🧠 Как работает логика

1. Расширение получает активную вкладку.
2. Проверяет, входит ли домен в белый список.
3. Если домен разрешён:
   - сбрасывает `distractedTime`;
   - обновляет `lastAllowedTime`.
4. Если домен не разрешён:
   - увеличивает `distractedTime`;
   - при превышении лимита времени — открывает `blocked.html`.
5. Если `enabled = false`, фоновая логика не выполняется.

---

## 🧩 Структура проекта

```text
focus-cat/
├─ manifest.json        # Manifest V3
├─ background.js        # Основная логика проверки/таймера/блокировки
├─ content.js           # UI-реакции в странице (таймер/кот)
├─ content.css          # Стили для content script
├─ popup.html           # Popup интерфейс
├─ popup.js             # Логика popup (время + ON/OFF)
├─ options.html         # Экран настроек доменов
├─ options.js           # Добавление/удаление whitelist-доменов
├─ blocked.html         # Страница блокировки
└─ icon.png             # Иконка расширения
```

---

## 🚀 Установка (локально, для разработки)

1. Откройте Firefox.
2. Перейдите на страницу: `about:debugging#/runtime/this-firefox`.
3. Нажмите **Load Temporary Add-on**.
4. Выберите файл `manifest.json` в корне проекта.

> После перезапуска браузера временное расширение нужно загрузить заново.

---

## ⚙️ Использование

### 1) Настройка белого списка
- Откройте popup → **Настройки**.
- Добавьте домены (например: `github.com`, `developer.mozilla.org`).

Поддерживаются «сырой» ввод и нормализация:
- `https://github.com/`
- `github.com:443`
- `*.github.com`

Все варианты будут приведены к корректному доменному виду.

### 2) Переключатель Focus
- В popup есть кнопка **Focus: ON / OFF**.
- Цвета состояния:
  - 🟢 ON — расширение активно.
  - 🔴 OFF — расширение выключено (логика не выполняется).

### 3) Блокировка
- Если активная вкладка не из белого списка дольше лимита, откроется `blocked.html`.

### 4) Лимит времени
- На странице `options` можно задать лимит отвлечения в минутах.
- Значение сохраняется в `browser.storage.local` как `limitMinutes`.

---

## 🔐 Разрешения

В `manifest.json` используются:

- `tabs` — чтение/обновление активной вкладки.
- `storage` — хранение настроек и состояния.
- `notifications` — уведомления (если используются в UI-логике).
- `host_permissions: <all_urls>` — работа с любыми сайтами.

---

## 🛠️ Идеи для развития

- [x] Настраиваемый лимит времени через `options`.
- [ ] Отдельные лимиты по доменам/категориям.
- [ ] Статистика по дням/неделям.
- [ ] Экспорт/импорт настроек.
- [ ] Локализация интерфейса (i18n).

---

## 📄 Лицензия

Потом добавлю

---


